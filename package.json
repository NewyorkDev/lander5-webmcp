{
  "name": "act-lander5-webmcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "benchmark": "node scripts/benchmark-lander5.mjs --iterations=25 --output=artifacts/lander5-benchmark.json",
    "screenshots": "node scripts/capture-submission-images.mjs",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "vite": "latest",
    "react": "latest",
    "react-dom": "latest"
  },
  "devDependencies": {
    "vitest": "latest"
  }
}
